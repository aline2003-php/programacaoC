#include <stdio.h>  // Includes the standard input/output library (required for printf)

int main(){         // The main function where program execution starts
    printf("Olá mundo!\n");  // Prints "Olá mundo!" to the console, \n adds a newline
    return 0;       // Returns 0 to indicate successful program execution
}
